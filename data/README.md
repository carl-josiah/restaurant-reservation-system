# data folder

This folder prevents data from being lost when the program closes.
It gets stored in these JSON files. These files are not edited manually.
The persistence folder handles that.

TL;DR: It's the system's long-term memory. Can't risk amnesia, you know?
